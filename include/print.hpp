#include <fstream>
#include <iostream>
#include <string>
//updated in main
void
print(const std::string& text, std::ofstream& out);
void
print(const std::string& text, std::ostream& out = std::cout);
